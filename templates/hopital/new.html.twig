{% extends 'base.html.twig' %}

{% block title %}New Hopital{% endblock %}

{% block body %}
    <h1>Create new Hopital</h1>

    {{ include('hopital/_form.html.twig') }}
    <div style="display: flex; justify-content: center; margin-top: 20px;">
       <a href="{{ path('app_hopital_index') }}" style="text-decoration: none; padding: 10px 20px; background-color: green; color: #fff; border-radius: 5px;">back to list</a>
    </div>

    
{% endblock %}
