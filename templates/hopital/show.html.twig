{% extends 'base.html.twig' %}

{% block title %}Hopital {% endblock %}

{% block body %}
    <h1>Hopital</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ hopital.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ hopital.adresse }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ hopital.ville }}</td>
            </tr>
            <tr>
                <th>Nbr_Chambre</th>
                <td>{{ hopital.nbrChambre }}</td>
            </tr>
            <tr>
                <th>Num_Tel</th>
                <td>{{ hopital.numTel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_hopital_index') }}">back to list</a>

    <a href="{{ path('app_hopital_edit', {'id': hopital.id}) }}">edit</a>

    {{ include('hopital/_delete_form.html.twig') }}
{% endblock %}
